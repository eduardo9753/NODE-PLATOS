require('./server');
/*const app = require('./server');
require('./database/database');//IMPORTAMOS LA CONEXION

//RUNNING SERVER
app.listen(app.get('port') , () => {
    console.log('Running Server: ' , app.get('port'));
});*/